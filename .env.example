# ============================================
# DESARROLLO LOCAL
# ============================================
PORT=3001
DB_USER=postgres
DB_PASSWORD=root
DB_HOST=localhost
DB_PORT=5432
DB_NAME=escuela_futbol
JWT_SECRET=your_super_secret_key

# ============================================
# RENDER (Produccion)
# Si existe DATABASE_URL, se usa esa conexion
# Si no, se usan las variables DB_* de arriba
# ============================================
# DATABASE_URL=postgresql://user:pass@host:5432/dbname
